{
    "default": [
        "build",
        "deploy"
    ],

    "build": [
        "jsonlint",
        "copy:vendors",
        "styles",
        "scripts",
        "svg_sprite"
    ],

    "styles": [
        "sass",
        "postcss",
        "cssmin"
    ],

    "scripts": [
        "concat",
        "jscs",
        "jshint",
        "uglify"
    ],

    "sync": [
        "browserSync",
        "watch",
        "notify:watch"
    ],

    "deploy": [
        "copy:www",
        "notify:www"
    ]
}
