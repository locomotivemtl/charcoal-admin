 {{!--
    Map sidebar
--}}

<div class="card" id="{{ widgetId }}">
    <div class="card-body" id="{{ widgetId }}">
        <div class="js-map-maker-map" style="height: 400px; width: 100%;"></div>
    </div>
</div>

{{# addJs }}
    <script>
    Charcoal.Admin.manager().add_widget({
        id: '{{ widgetId }}',
        type: 'charcoal/admin/widget/map',
        data: {{& widgetDataForJsAsJson }}
    });
    </script>
{{/ addJs }}
