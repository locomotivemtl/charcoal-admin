{{!--
    Table Widget \ Empty State
    ==========================

    @used-by `charcoal/admin/widget/table`
--}}
<div class="alert alert-danger" role="alert">
    <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
    <span class="sr-only">{{# _t }}Erreurs:{{/ _t }}</span>
    <span>Aucun objet de type "{{ objType }}" n'existe.</span>
    {{# isObjCreatable }}
    <div class="btn-group" role="group">
        <a class="btn btn-info" href="{{ objectCreateUrl }}">{{# _t }}Créer un nouvel objet{{/ _t }}</a>
    </div>
    {{/ isObjCreatable }}
</div>
