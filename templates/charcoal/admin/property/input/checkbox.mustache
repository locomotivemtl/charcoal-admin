{{!--
    Checkbox Property Input
    =======================

    `charcoal/admin/property/input/checkbox`
--}}
{{> charcoal/admin/property/control/fallback }}
{{# choices }}
<div class="checkbox{{# inputLayouts.inline }} checkbox-inline{{/ inputLayouts.inline }}">
    {{> charcoal/admin/property/control/choice }}
    <label for="{{ inputId }}">{{& label }}</label>
</div>
{{/ choices }}
