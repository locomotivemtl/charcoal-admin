{
    "default": [
        "build",
        "deploy"
    ],

    "build": [
        "copy:vendors",
        "sass",
        "postcss",
        "cssmin",
        "concat",
        "jscs",
        "jshint",
        "jsonlint",
        "uglify",
        "svg_sprite"
    ],

    "sync": [
        "browserSync",
        "watch",
        "notify:watch"
    ],

    "deploy": [
        "copy:www",
        "notify:www"
    ]
}
