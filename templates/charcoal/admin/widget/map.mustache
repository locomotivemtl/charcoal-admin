{{!--
    Map Widget
--}}

<div id="{{ widgetId }}" class="card">
    <div class="card-body">
        <div id="{{ widgetId }}_map" class="js-map-div" style="height: 400px; width: 100%;"></div>
    </div>
</div>

{{# addJs }}
    <script>
    Charcoal.Admin.manager().add_widget({
        id:   '{{ widgetId }}',
        type: 'charcoal/admin/widget/map',
        data: {{& widgetDataForJsAsJson }}
    });
    </script>
{{/ addJs }}